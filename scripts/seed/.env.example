# Copy to .env and fill in. The secret key is in the Supabase dashboard
# under Project Settings -> API Keys. NEVER commit the real .env.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...

# Optional: an existing account (by email) to promote to admin after seeding
ADMIN_EMAIL=
